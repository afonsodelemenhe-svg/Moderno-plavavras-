module.exports = async function log(message, client) {
  const logChannel = client.channels.cache.get("ID_DO_CANAL_DE_LOGS");

  if (!logChannel) return;

  logChannel.send(
    `🛑 Palavra proibida detectada!\nAutor: ${message.author.tag}\nMensagem: ${message.content}`
  );
};
